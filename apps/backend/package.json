{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "pnpm run deploy:firestore && pnpm run deploy:functions",
    "deploy:firestore": "npx firebase-tools deploy --only firestore:rules,firestore:indexes",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules",
    "deploy:functions": "pnpm --filter backend-functions run build && npx firebase-tools deploy --only functions",
    "test:rules": "pnpm exec firebase emulators:exec --only firestore \"node --test tests/firestore-rules.test.mjs\""
  }
}
